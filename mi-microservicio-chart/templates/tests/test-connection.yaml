apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mi-microservicio-chart.fullname" . }}-test-connection"
  labels:
    {{- include "mi-microservicio-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mi-microservicio-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
